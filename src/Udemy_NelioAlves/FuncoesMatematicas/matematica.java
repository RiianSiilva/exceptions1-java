package Udemy_NelioAlves.FuncoesMatematicas;

public class matematica {
    static void main() {
        // A = Math.sqrt(x); == variavel A recebe a raiz quadrada de x
        // A = Math.pow(x, y) == variavel A recebe o resultado de x elevado a y
        // A = Math.abs(x) == variavel A recebe o valor absoluto de x, que sign um valor sem o negativo
    }
}
